# Portfolio website

## Todo

- [ ] Folder creations: (Make a server action that takes a name and parentid, creates a folder with that name and parent, also dont forget to set the ownerId)
- [ ] Folder deletions: (Make a server action that takes a folderid, deletes all files with the folderid and finally deletes the folder)
- [ ] Folder renames: (Make a server action that takes a folderid and updates the name field)
- [ ] Fix onboarding UI and add logos
- [ ] Access control: /f/[folderid] page needs to check if the user is authenticated and if so display correct folders (redirect if not authed).
- [ ] Make a file view page.
- [ ] Add toasts and general UX additions.
- [ ] Gray out a row while it's being deleted.
